

class TrackAnalyticsEvent {
    constructor(accountId, analyticsEvent = null, fingerprint = null) {
        // more code to create track analytics event command...
        this.account_id = accountId;
    }
}

module.exports = TrackAnalyticsEvent;